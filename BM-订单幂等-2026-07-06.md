# 技术评审纪要

- **主题**：订单幂等设计
- **参与**：架构侧 / 后端侧
- **日期**：2026-07-06

## 讨论要点
1. 现状"前端置灰 + Redis SETNX 挡 3 秒"只防手抖，超时重试/网络抖动仍会穿透，本质无幂等。
2. 核心方案：**客户端幂等号 + 服务端去重表（唯一索引）**。结算页生成 UUID，重试复用；插入成功才走真正下单，冲突即重放。
3. 冲突处理要**幂等回放**：去重表存首次订单号/响应，命中原样返回，避免调用方误判失败再重试。
4. "插幂等记录 + 建订单"必须**同一事务**，否则记录已插而单未建，重试被永久挡死。
5. 幂等号**全链路透传**：库存、支付各自本地去重；MQ 按幂等号做消费幂等防重复投递。
6. 中间态用**对账兜底**：支付回调缺失时定时任务查真实状态补偿/关单。

## 结论共识
- 幂等号唯一索引离散、不构成热点；真正热点在库存行，走**分段库存 + 短事务**与幂等解耦。
- 分层落地：入口幂等号去重 → 事务内建单 → 下游透传去重 → 对账补偿。

## 遗留问题
- etcd/DB 唯一约束选型与峰值 QPS 压测。
- 幂等号 TTL 与去重表清理策略。

## 建议补充的具体信息
- 峰值下单 QPS、支付回调平均/最坏时延，用于定对账扫描频率。
- 现有支付/库存下游是否已支持幂等号透传字段。
